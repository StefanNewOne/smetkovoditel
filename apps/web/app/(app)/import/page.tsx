import { ScreenPlaceholder } from "@/components/screen-placeholder";

export default function ImportPage() {
  return (
    <ScreenPlaceholder
      title="Import центар"
      phase="Ф2"
      backlogId="SM-30 … SM-35"
      summary="НЛБ изводи (multi-upload + Gmail авто) со интегритет линија (B14), и 4 живи редици: извод-линии, receipts без линија, FACEBK без receipt, PARTIAL parse — секоја со акции за решавање."
    />
  );
}
